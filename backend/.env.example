# Backend Server Configuration
PORT=3000
NODE_ENV=production

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Supabase Configuration (for email service & database)
VITE_SUPABASE_URL=https://your-project-url.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Email Service Configuration (Nodemailer SMTP)
SMTP_HOST=mail.yourdomain.com
SMTP_PORT=587
SMTP_USER=your_email@yourdomain.com
SMTP_PASSWORD=your_email_password
SMTP_FROM_EMAIL=noreply@yourdomain.com
SMTP_FROM_NAME=JKLG Travel

# Frontend URL (for email verification and password reset links)
FRONTEND_URL=http://localhost:5173

# Stripe Configuration (Payment Processing)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_stripe_webhook_secret_here

# Setup Instructions:
# 1. Get VITE_SUPABASE_URL from Supabase project settings
# 2. Get SUPABASE_SERVICE_ROLE_KEY from Supabase Settings > API > Service Role Key
# 3. Configure SMTP credentials with your email provider
# 4. Generate JWT_SECRET (min 32 chars): node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 5. Get Stripe keys from https://dashboard.stripe.com/apikeys
# 6. Set up Stripe webhook endpoint to POST /api/payments/process-webhook
